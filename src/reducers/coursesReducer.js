import { FETCH_COURSE } from './types';

export default function(state = [], action) {
    switch (action.type) {
        case FETCH_COURSES:
            
            return state;
        default:
            return state;
    }
}